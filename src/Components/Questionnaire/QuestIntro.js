import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function QuestIntro({ user, nextPage }) {
    return (
        <View>
            <Text>QuestIntro</Text>
        </View>
    )
}

const styles = StyleSheet.create({})